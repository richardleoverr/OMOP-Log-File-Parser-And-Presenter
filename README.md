# ETL Log Parser (Shiny)

A small Shiny app for parsing and comparing OMOP CDM ETL log files.

The app reads ETL log files and extracts:

- Run summary
- ETL runtimes per table
- Executed scripts
- Skipped scripts
- Errors

Multiple log files can be uploaded and are shown side-by-side.
